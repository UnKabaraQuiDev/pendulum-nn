#version 430

layout(local_size_x = 256) in;
layout(std430, binding = 0) buffer Buf { vec4 data[]; };

uniform uint elementCount;

void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i >= elementCount)
        return;
        
    data[i] = vec4(0.0);
}
